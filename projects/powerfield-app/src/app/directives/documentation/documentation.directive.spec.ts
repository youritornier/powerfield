import { DocumentationDirective } from './documentation.directive';

describe('DocumentationDirective', () => {
  // it('should create an instance', () => {
  //   const directive = new DocumentationDirective();
  //   expect(directive).toBeTruthy();
  // });
});
