import { AutowidthDirective } from './autowidth.directive';

describe('AutowidthDirective', () => {
  it('should create an instance', () => {
    const directive = new AutowidthDirective();
    expect(directive).toBeTruthy();
  });
});
